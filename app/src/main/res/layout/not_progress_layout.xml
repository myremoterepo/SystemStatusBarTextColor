<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="match_parent"
              android:gravity="center_vertical"
              android:paddingLeft="15dp">

    <ImageView
        android:id="@+id/not_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_launcher"/>


    <TextView
        android:id="@+id/not_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/not_icon"
        android:layout_marginLeft="20dp"
        android:text="@string/not_progress_title"
        android:textSize="16sp"/>

    <TextView
        android:id="@+id/not_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/not_title"
        android:layout_toRightOf="@id/not_icon"
        android:layout_marginLeft="20dp"
        android:text="@string/not_progress_content"
        android:layout_gravity="center_horizontal"/>

</RelativeLayout>